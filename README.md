# jenkins-pipelines
This repository hosts 3 brances for the: CI-CD-PIPELINES
1. CI_JOB - runs only the CI part
2. CD_JOB - runs only the CD part (after CI_JOB is finished)
3. CI_CD_JOB - runs both CI + CD parts

